{
  "name": "hello-collab-convex",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.1",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo dev --ui=tui",
    "build": "turbo build --filter=web",
    "lint": "turbo lint --filter=web",
    "codegen": "turbo codegen --filter=web"
  },
  "devDependencies": {
    "turbo": "^2.8.10"
  }
}
